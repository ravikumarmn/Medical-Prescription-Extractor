from setuptools import setup, find_packages

setup(
    name="extractortool",
    version="0.1.0",
    description="A prescription extraction tool",
    packages=find_packages(),  # Ensure you have a package directory named 'extractor'
)
